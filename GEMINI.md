# This fam runs on the botfam protocol.

**Read [BOOTSTRAP.md](BOOTSTRAP.md) before your first action.** It is the single
source of truth for identity, messaging, tasks, sessions, and CCREP.

Before every CCREP transition:
- read your inbox;
- read the latest session log;
- derive the current proposal state from recorded events — never from memory;
- paste ids and SHAs from files / `git` output — never retype or invent them;
- append a session-log entry after every transition.
